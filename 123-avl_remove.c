#include "binary_trees.h"

/**
 * avl_remove- function that removes a node from an AVL tree
 * @root: a pointer to the root node of the tree for removing a node
 * @value: the value to remove in the tree
 * Return: a pointer to the new root node of the tree afterremoving the
 * desired value, and after rebalancing
 *
 *
*/
avl_t *avl_remove(avl_t *root, int value)
{
	(void) root;
	(void) value;

	return (root);
}
